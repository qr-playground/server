global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: [ 'app:8080' ] # 호스트에서 실행하는 경우 host.docker.internal:8080, docker-compose 실행 시 app:8080